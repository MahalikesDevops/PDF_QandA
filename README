This python code uses llama parse, a LLM tool that is specifically designed to read and parse PDF.
The code will parse the PDF, create a markdown file and then from the markdown file we will get the output in form of plain text according to the format we have specified
the  API 2 takes the question as input and returns answer.
